adding the attribute id="IdNoSpaces" to an element gives it an ID
ID is global, and must be unique, no spaces, use sparingly or not at all
 use #IdNoSpaces as the SELECTOR

adding the attribute class="ClassNamesSpaceSeperated" to an element identifies it as part of one of the listed groups
 Use .ClassName as the SELECTOR
